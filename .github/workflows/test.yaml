name: Test

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        type: environment

jobs:
  build:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: print sucess
        run: |
          echo "${{vars.project_name}} Success"
      
  
